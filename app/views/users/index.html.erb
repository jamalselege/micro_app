<h1>Listing users</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |micropost| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Show', user_micrpos_path(@user,micropos) %></td>
    <td><%= link_to 'Edit', edit_user_micropo_path(@user,micropos) %></td>
    <td><%= link_to 'Destroy', user_micropost_path(@user,micropos), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
